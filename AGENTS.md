# JLPT 学习系统协作规则

当学习者在此项目中发送日语学习内容时：

1. 将其视为学习者吸收后的主动输出，先检查理解、接续、例句和疑问。
2. 保留学习者原文；不得以校对内容替换原文。
3. 输出校对后的待确认卡，补充语义边界、易混项、JLPT 考点和自然例句。
4. 学习者给出礼貌体语法时，卡片的“核心”和“接续”也保留礼貌体；语法形式优先用平假名，并在括号中标注对应汉字，例如`だします(出します)`。
5. 只有学习者明确说“确认入库”后，才创建或修改正式卡，并从确认日开始七天复习周期。
6. 复习固定每七天一次；答错和“不确定”只能改变错题标签与未来出题侧重，不得增加提前复习。
7. 测验仅覆盖 JLPT N2 非听力题型。每道四选一题必须解释正确项和其余三个选项。
8. 词汇题答错时创建待确认候选卡；未经学习者确认不得进入复习。

在执行任何读写操作前，先阅读 `START-HERE.md`、资料库根目录的 `README.md` 和相应卡片的 YAML/JSON 前置元数据。

## 从视频生成语法卡

学习者要求根据课程视频或播放列表整理语法卡时，先阅读并遵守 [视频语法待确认卡流程](docs/video-grammar-workflow.md)。执行范围由学习者手动指定；优先用日本的日语教学网站独立复核，接续写法参考已有正式卡，截图放在“视频学习资料整理”中。关键内容无法核实则列出问题、不交付该课卡片；只有明确“确认入库”后才创建或修改正式卡并启动七天复习。

## 工具约定

Python 一律通过 uv 执行：`uv run python <脚本>`。本机 PATH 中没有系统 Python（只有微软商店占位符），uv 托管的解释器仅能经 `uv run` 使用，直接调用 `python`/`python3` 会报错。
