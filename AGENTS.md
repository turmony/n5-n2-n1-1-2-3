# JLPT 学习系统协作规则

当学习者在此项目中发送日语学习内容时：

1. 将其视为学习者吸收后的主动输出，先检查理解、接续、例句和疑问。
2. 保留学习者原文；不得以校对内容替换原文。
3. 输出校对后的待确认卡，补充语义边界、易混项、JLPT 考点和自然例句。
4. 学习者给出礼貌体语法时，卡片的“核心”和“接续”也保留礼貌体；语法形式优先用平假名，并在括号中标注对应汉字，例如`だします(出します)`。
5. 只有学习者明确说“确认入库”后，才创建或修改正式卡，并从确认日开始七天复习周期。下述视频语法流程允许把待确认内容提前写入正式卡目标路径作为未提交工作区版本；这不构成确认，不得提前创建确认事件或启动复习。
6. 复习固定每七天一次；答错和“不确定”只能改变错题标签与未来出题侧重，不得增加提前复习。
7. 测验覆盖 JLPT N5–N1 非听力题型。学习者指定语法后，优先检索日本日语教育网站已有练习题；原题不足时，允许根据已核实的语法讲解和自然例句制作可追溯的派生题。派生题必须明确标识，不得冒充网站原题、官方题或历年真题，并须通过唯一解检查及最多两轮修订后的独立盲审。每道四选一题必须解释正确项和其余三个选项。
8. 词汇题答错时创建待确认候选卡；未经学习者确认不得进入复习。

在执行学习资料相关的读写操作前，先阅读资料库根目录的 `README.md`、用户指定或任务对应的流程文件，以及相应卡片的 YAML/JSON 前置元数据。

## 从视频生成语法卡

学习者要求根据课程视频或播放列表整理语法卡时，先阅读并遵守 [视频语法待确认卡流程](docs/video-grammar-workflow.md)。执行范围由学习者手动指定；优先用日本的日语教学网站独立复核，接续写法参考已有正式卡，截图放在“视频学习资料整理”中。关键内容无法核实则列出问题、不交付该课卡片。完整待确认内容直接写入正式卡目标路径但保持未提交，后续反馈继续修改同一文件；只有明确“确认入库”后才更新确认记录并启动七天复习。

## 工具约定

### 图片链接与入库校验

- 视频语法卡必须在把待确认正文写入正式卡目标路径前校验图片链接；所有入库方式（函数、批量脚本、手动整理）都必须在写确认事件前再次校验当前正文。已按目标目录写好的正文调用 `AssetIndex(root).check(destination, body)`；其他流程确需跨目录转卡时调用 `jlpt_notes.asset_links.rebase_assets(root, source, destination, body)`，其中两个文档路径相对于资料库根目录。有问题则停止写入。
- 正文图片和图片超链接以当前 Markdown 所在目录为基准；`source.screenshot` 元数据仍以资料库根目录为基准。不得全局替换 `../assets/`，不得根据同名文件猜测目标。
- 视频语法待确认版本写入正式卡目标路径后，以及任何卡片确认入库完成后，均运行 `uv run python -m jlpt_notes validate-links --root jlpt-notes`；退出码非零时列出问题，不宣称完成。该命令只读，完整 pytest 测试也会检查实际资料库。
- 图片技术修复不重启复习周期，不改学习者原文、确认日期、复习日期或确认事件。

Python 一律通过 uv 执行：`uv run python <脚本>`。本机 PATH 中没有系统 Python（只有微软商店占位符），uv 托管的解释器仅能经 `uv run` 使用，直接调用 `python`/`python3` 会报错。
