# 新聊天从这里开始

你可以在同一个项目下新建 Codex 聊天，并直接发送当天学习后的日语输出。新聊天会读取 `AGENTS.md` 来遵守本系统的学习规则。

建议按以下格式发送：

```text
等级：N3
课程序号：第 12 个语法点
语法：～わけではない
我的理解：……
视频中的例句／我记下的例句：……
我的疑问：……
```

收到校对后的待确认卡后，说“确认入库”即可开始该卡的固定七天复习。系统尚在建设期间时，聊天仍可检查和整理你的学习内容，但完整的本地自动记录功能需要待基础实现完成后使用。

## 根据视频整理语法卡

提供本次要整理的课程序号即可使用已保存的出口日語 N3 课程；整体待整理范围为第31—120课，但每次只执行你指定的范围。流程见 [视频语法待确认卡规范](docs/video-grammar-workflow.md)：先整理并独立复核，接续参考已有卡片，截图放在“视频学习资料整理”中；你审核后明确“确认入库”，才开始七天复习。关键内容无法核实的课程只列问题，不交付卡片。

## 在 iPad 阅读资料

阅读器只在你手动打开时运行，不会随电脑启动。它不要求 Windows 或阅读器密码，但同一家庭局域网中的其他设备也可能看到资料，因此只应在可信任的家庭网络使用。

1. 确保 Win11 和 iPad 连接同一个家庭 Wi-Fi，并将该 Windows 网络标记为“专用网络”。访客 Wi-Fi 通常无法连接。
2. 双击桌面的“JLPT iPad 阅读器”。如果尚未安装，请先明确批准首次设置；Codex 会依次完成“安装阅读器（稳定目录）→ 创建桌面快捷方式 → 配置 Private + LocalSubnet 防火墙”。
3. 等待状态变为“运行中”。若窗口提示尚未配置局域网访问，点击“配置专用网络访问”，确认 Windows 提示；该操作只需在首次使用或规则失效后执行。
4. 点击“复制地址”，在同一 Wi-Fi 的 iPad Safari 中打开。也可在 Safari 分享菜单中选择“添加到主屏幕”。
5. 阅读结束后关闭 Win11 控制窗口；访问地址会立即停止服务。

阅读器严格只读：它不会修改语法卡、草稿、复习记录或七天复习日期（试卷的答案填写区是唯一例外，见下段）。源文件更新后页面会在数秒内更新；当前正在阅读的页面会先提示，避免强制跳动。电脑休眠、程序关闭或 iPad 离开当前局域网后均无法访问。阅读器不提供离线缓存。

试卷可以直接在 iPad 上作答：打开试卷页后点选选项（排序题按顺序点选），需要时打开「标记不确定」，全部作答后点「提交答案」。答案会写入试卷尾部的答案填写区，供判分使用。已提交的试卷页只显示答案，不能重复提交；作答中途刷新页面不会丢失进度。

首次设置只允许使用稳定项目目录 `D:\PersonalProject\n5-n2-n1-1-2-3`，不能使用路径中含 `.worktrees` 的临时开发副本。安装时使用同一固定运行时中的 `python.exe` 和同目录的 `pythonw.exe`；快捷方式与防火墙必须指向这个完全相同的 `pythonw.exe`。如果项目或运行时位置改变，请让 Codex 从稳定目录重新安装、验证实际导入位置、重建快捷方式，并在需要时替换防火墙规则。
